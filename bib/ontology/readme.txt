Process for deploying a new version of the ontology:

1. Generate new documentation. See ../documentation/LODE/readme.txt.

2a. Copy to bib.ld4l.org server into directory /libweb/sites/bib.ld4l.org/htdocs,
and rename as ontology.rdf.

Or

6b. Check out the ld4l-ontology repository from github and update to get the 
latest version. Then copy the file to the directory in 2a.